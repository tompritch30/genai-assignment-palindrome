[pytest]
# Pytest configuration
# Only run tests in tests/ directory, excluding llm_tests/ subfolder
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
# Exclude llm_tests subfolder (requires API key)
norecursedirs = llm_tests